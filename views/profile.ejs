<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
</head>
<body>
    <div class="min-h-screen w-full bg-zinc-900 p-10 text-white">
        <div class="flex justify-end">
            <a href="/logout" class="bg-red-500 rounded-lg mr-10 px-4 py-2">Logout</a>
        </div>
        <div class="flex items-start gap-3 mb-5">    
             <div class="h-10 w-10 overflow-hidden object-cover rounded-md">
                <img src="/images/uploads/<%=user.profilepic%>" alt="img">
             </div>
             <h1 class="text-3xl tracking-tighter mb-3"> Hello ! <%=user.name%> </h1>
        </div>     
        <h3 class="mb-5">Create Post</h3>
        <form action="/post" method="post">
            <textarea class="outline-none bg-zinc-600 w-1/3 rounded-lg resize-none p-5 block" placeholder="What's in your mind ?" name="content" ></textarea>
            <input type="submit" value="Post" class="px-10 py-1 bg-blue-500 outline-none rounded-md mt-4 ">
        </form>

        <div class=" mt-20">
            <h3 class="text-2xl tracking-tighter mb-8">You can checkout Post Here !</h3>
                <%user.posts.reverse().forEach(function(post){%>
                    <div class="bg-zinc-700 w-1/3 rounded-lg p-5 mb-5"> 
                        <h2 class="text-xl mb-3">@<%=user.username%></h2>
                        <p><%=post.content%></p>
                        <large class=" mt-2 inline-block"><%=post.likes.length%> like </large>
                        <div class="flex gap-6 mt-3">
                            <a href="/like/<%=post._id%>" class="text-blue-500">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
                            </a>
                            <a href="/edit/<%=post._id%>" class= "text-zinc-500">Edit</a>
                        </div>
                    </div>
              <%  }) %>
        </div>
    </div>
</body>
</html>